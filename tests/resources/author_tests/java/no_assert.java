import org.junit.Test;

public class MyTests {

    @Test
    public void testWithoutAssertion() {
        // 测试逻辑，但没有断言
        int result = someMethod();
        // 没有验证结果
//         assertTrue(result > 0);
    }
}
